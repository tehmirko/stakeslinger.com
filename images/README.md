# images/

Add brand assets:

- `logo.png` — main logo (used in header + hero; transparent PNG, ~512px tall)
- `og-image.png` — social share card, 1200×630px

When the logo is available, drop it here. The site references `/images/logo.png`
and falls back gracefully (hidden) if missing.
